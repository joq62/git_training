
1) Create push and update
	* git add *
	* git commit -m "comment"
	* git push

2) Continue to develop but don't want to use it wrong path  	
	* git checkout -b branchname
	* git add *
	* git commit -m "comment"
	* git push -u branchname

3) Satisfied - merger branch
	* git checkout master
	* git merge branchname
	* git push 

4) Use tags for getting right versions when downloadng SW
	* git tag -a v1.0.0 -m "comment"
or
	* git tag v1.0.0
	* git -l -n3

	* git push origin tag
